# Third-party reference material (not redistributable)
reference.pdf

# Personal docs (per-user context, progress logs, research-direction seed,
# venue strategy, etc. — see CLAUDE.md "개인 컨텍스트" section)
docs/private/

# Per-user experiment cards. Only the public sample + README are tracked.
projects/*
!projects/example/
!projects/README.md

# Per-user explore directions + findings (docs/explore-schema.md).
explore/

# Per-user Claude Code session settings
.claude/settings.local.json

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
*.egg-info/
.venv/
venv/

# Node
node_modules/
dist/
.vite/

# Editors / OS
.DS_Store
.idea/
.vscode/
*.swp

# Compose / Docker overrides + env
docker-compose.override.yml
.env

# Run artifacts (proj-level — keep card.md, drop heavy outputs)
projects/*/runs/
projects/*/results/
projects/*/checkpoints/
